# Kit defects — the replication trial log

This instance is the TMS Kit's first replication from the kit alone. Every question a session had to
ask, every step of `kit-docs/00_REPLICATION_RUNBOOK.md` that did not work as written, and every place
the scaffold and the docs disagree is a defect **in the kit**, not in this project. Log it here in
the moment; the kit's maintainers fold it back.

Format, newest first:

```
## YYYY-MM-DD · <doc or file> · <one line>
What was expected · what happened · what was done here · what the kit should change.
```

---

_(empty — nothing replicated yet)_

## 2026-09-18 · db/migrations/0147_assessor_materialised.sql · kit migration is un-appliable on a clean DB
Expected: migrations apply forward-only on a fresh `migrate -> seed` (root CLAUDE.md §5) · Found: 0147's final assertion RAISE EXCEPTIONs when mv_assessor_residual is empty, which it always is at migrate time on a clean build (seed:history runs after migrate), so reset:clean aborts at 0147 · Done here: kept the amended migration - the emptiness check was relaxed to a NOTICE with an n_src guard - and recorded the amendment in the file header, because a later migration cannot rescue an earlier abort and a checksum waiver in migrate.mjs is the escape hatch the project forbids · Kit should: make the emptiness check pipeline-conditional (NOTICE when the corpus itself is empty) in the migration as shipped, not as a post-hoc guard.

## 2026-09-08 · scaffold/ · no Dockerfile ships with the kit
Expected: `docs/15_DEPLOYMENT.md` and `deploy/compose.yml` assume an image `APP_IMAGE` built from the scaffold · Found: no `Dockerfile` anywhere in `scaffold/` · Done here: a Dockerfile will be written in Phase 4 against `docs/15` · Kit should: ship `scaffold/Dockerfile` (multi-stage, Node 22, `APP_RELEASE` build arg) and reference it in `deploy/README.md`.

## 2026-09-08 · scripts/*.mjs · node scripts do not load `.env`
Expected: `README.md` §Quickstart implies `npm run migrate` works after `cp .env.example .env` · Found: every script reads `process.env` directly; only `next dev` loads `.env`, so `migrate`/`reset`/`verify` fail with "DATABASE_URL is not set" on a workstation · Done here: `package.json` scripts prefixed with `node --env-file=.env` (Node ≥ 20.6) · Kit should: do the same, or document `export $(cat .env)` per shell.

## 2026-09-08 · .env.example · one `DATABASE_URL` / `PDF_URL` for two contexts
Expected: a single `.env` serves both `next dev` on the host and the app container · Found: the container needs `@db:5432` and `http://pdf:3000`, the host needs `@localhost:<DB_PORT>` and `http://localhost:<PDF_PORT>`; nothing says so · Done here: host values in the PC `.env`, container values in the server `.env` (one `.env` per host anyway) · Kit should: state this in `.env.example` §4 and §7.

## 2026-09-08 · _KIT_CONTRACT.md:68 · names a specific REST-over-Postgres product
Expected: the kit's neutrality claim ("no LMS or vendor") · Found: one product name survives in the contract · Done here: reworded · Kit should: add the term to its own scan.

## 2026-09-08 · deploy/initdb/00_bootstrap.sql · schema-owner role name is hard-coded
Expected: `DB_MIGRATION_ROLE` in `.env.example` names the role · Found: the SQL hard-codes `tms_owner` regardless · Done here: renamed to `ava_owner` in the SQL and set `POSTGRES_USER=ava_owner` so the bootstrap's CREATE ROLE is a no-op and the same role owns and migrates · Kit should: either template the role name or drop `DB_MIGRATION_ROLE` from `.env.example`.

## 2026-09-08 · deploy/compose.yml · optional profiled services carry `:?` required variables
Expected: `docker compose up -d db pdf` works with only the db/pdf variables set (README quickstart) · Found: Compose interpolates every service at parse time, so `AUTOMATION_IMAGE:?` and `INFERENCE_IMAGE:?` abort the command even though neither profile is enabled · Done here: both defaulted to a clearly-named placeholder; `AUTOMATION_ENCRYPTION_KEY` likewise · Kit should: do the same, or move the optional services to a separate overlay.

## 2026-09-08 · README.md / deploy/README.md · `.env` is not where Compose looks for it
Expected: `.env` in `scaffold/` serves both `npm run …` and `docker compose -f deploy/compose.yml` · Found: Compose resolves `.env` relative to the first compose file's directory (`deploy/`), so every variable is missing · Done here: every compose invocation carries `--env-file .env` from `scaffold/` · Kit should: state it in the quickstart, or add `--project-directory .` to the documented commands.

## 2026-09-08 · next.config.mjs · `eslint` key rejected by Next 16
Expected: clean `next dev` · Found: "eslint configuration in next.config.mjs is no longer supported" · Done here: key removed · Kit should: remove it.

## 2026-09-08 · deploy/compose.yml · the base file cannot start db/pdf alone on a workstation
Expected: runbook Phase 1 says `docker compose -f scaffold/deploy/compose.yml up -d db` · Found: the `app` service's `${APP_IMAGE:?…}` guard aborts interpolation for every service, so the documented command can never work without an overlay — and the overlays require a built image the workstation does not have · Done here: `deploy/compose.dev.yml`, a standalone db + pdf file with the same project/network/container names, used only on the PC · Kit should: ship the same, and point Phase 1 of the runbook at it. The guard on the base file is right and stays.

## 2026-09-08 · scripts/seed-framework.mjs:37, scripts/build-framework-seed.py:26 · blueprint folder name is hard-coded
Expected: the runbook lets the kit be copied "from this folder alone" into any layout · Found: both scripts resolve `<kit>/docs/03_COMPETENCY_FRAMEWORK.md` by literal name, so a replica that keeps its own `docs/` (as this one does — the blueprint lives in `kit-docs/`) fails at seed 3/6 · Done here: `KIT_DOCS_DIR` env var, default `kit-docs` · Kit should: read the folder from one place (env or a `kit.json`), default `docs`.

## 2026-09-08 · runbook Phase 3 / scripts · no way to sign in to a freshly seeded instance
Expected: runbook Phase 3 says "Sign in, open the subject list" · Found: every seeded login carries the deliberate never-matching hash `!` and the kit ships no tool to set a password, so Phase 3 cannot be executed as written · Done here: `scripts/set-password.mjs` (`npm run password -- <username>`), hidden prompt, bcrypt computed in Postgres like the login route · Kit should: ship the same and name it in the runbook. Seeded staff logins are `demo.platform_admin`, `demo.training_manager`, `demo.assessment_manager`, `demo.records_officer`, `demo.qms_admin`, `demo.compliance_verifier`, `demo.planner`.

## 2026-09-08 · scripts/smoke-screens.mjs, .env.example §6 · the `x-internal-token` bypass is documented but not implemented
Expected: `.env.example` says the header "is checked before any route logic runs" and the smoke sends it · Found: neither `proxy.ts` nor `requireSession()` reads it, so `/api/subjects` returns 401 and the smoke aborts on every fresh install · Done here: the smoke signs in as a real user (`SMOKE_USER` / `SMOKE_PASSWORD`, local `.env` only) and carries the session cookie; no bypass added · Kit should: either implement the header in `proxy.ts` + `requireSession()` with a constant-time compare, or drop it from `.env.example` and do what this replica does.

## 2026-09-08 · scripts/load-analytics-config.mjs · policy.yaml is only recorded by the synthetic seeder
Expected: `verify --group config` asserts "the recorded policy version is the file on disk" on any environment built by the documented path · Found: the `policy` row in `config_versions` is written by `seed-synthetic.mjs` alone, so an instance without a synthetic population can never pass that assertion, and a policy edit on a live instance is invisible to the gate until the population is regenerated · Done here: `config:load` records the policy version too (same `recordConfigVersion`) · Kit should: do the same — policy is configuration, not seed data.

## 2026-09-08 · template_kinds catalogue · no loader from policy.yaml
Expected: policy.yaml §1 says "Adding a kind is a row here and a row there, never a migration" · Found: the table is only ever written by migration 0032; nothing syncs it from the file, so the only way to add a kind IS a migration · Done here: `0141_template_kinds_operator.sql` (ebt_recurrent, type_rating; OPC / LPC relabel) · Kit should: either sync the catalogue in `config:load` (with the same is_active-never-DELETE rule) or correct the comment.

## 2026-09-08 · scripts/synthetic/population.mjs · org units and asset classes are only written by the synthetic generator
Expected: the operator context (bases, fleets, devices) exists on any instance, populated or not · Found: `org_units` and `asset_classes` are inserted only inside the synthetic population step, and the lists live under `policy.yaml → synthetic:` · Done here: `scripts/seed-operator.mjs` (`npm run seed:operator`), run in `reset:clean` before the first user; lists still read from `synthetic:` to avoid a policy schema change · Kit should: move the two lists to a top-level `operator:` section and seed them in `config:load` or a dedicated step.

## 2026-09-10 · tsconfig.json + 8 source files · `npm run typecheck` has never passed on the kit
Expected: the runbook's `typecheck` script is a gate · Found: 109 errors in kit files alone - the vitest test file has no test-runner types in `tsconfig` (86 errors), and `noUncheckedIndexedAccess: true` is set but eight files index arrays/regex groups without a guard (charts, db, inference, provenance, qms conditions/engine/plain-english) · Done here: `types: ["node", "vitest/globals"]` added; every indexed access guarded (no behaviour change: the guards return the same null/empty the surrounding code already handles) · Kit should: run its own typecheck in the gate suite; a typecheck that is never green is a check that has never failed.

## 2026-09-11 · vitest · `npm test` has never run a suite
Expected: `npm test` runs `lib/analytics/__tests__/screening-index.test.ts`, which is written against `describe / it / expect` as globals · Found: no `vitest.config.*` exists, so globals are off at runtime and the suite fails on its first line with "describe is not defined" — the types were added to tsconfig (defect 16) but the runtime was never told · Done here: `vitest.config.ts` with `globals: true` and the `@/` alias · Kit should: ship the file; a test command that has never run is the same defect as a typecheck that has never passed.

## 2026-09-11 · lib/analytics/screening-index.ts, its test · the critical grade could score
Expected: docs/06 §7 - a grade of 1 "drives the flag and never enters the arithmetic" · Found: once the suite actually ran (defect 17), the leniency adjustment clamped an awarded 1 up to `clamp_min` = 2, which carries -5 points, so a critical grade DID move the score; and the test fixture wrapped its dates inside one year, so a series longer than twelve events no longer sorted in index order and the window test read events it believed discarded · Done here: the critical grade bypasses adjustment at full weight; fixture dates roll into the next year past twelve · Kit should: take both; the code fix changes screening scores for any subject with a grade 1 and a qualifying assessor, and that is the correct direction.

## 2026-09-13 · app/change-password + app/api/auth/login · the documented account-creation flow dead-ends on first sign-in
Expected: an administrator creates an account, hands over the temporary password, the holder signs in and is made to change it · Found: `login/route.ts` redirects to `/change-password` whenever `must_change_password` is true, and `/change-password` is a `StubPage` — so every account created through the UI, which `lib/admin.ts` deliberately creates with the flag set, cannot be used at all. The two halves of the feature were shipped in opposite states: the enforcement exists, the page it enforces does not · Done here: `npm run password -- <username>` (defect 11's script) clears the flag by default, so the sequence is create in the UI, then set the password from the shell. Not built: the page is not in `docs/03_DEMO_SCRIPT.md`, and the rule here is that nothing off-script is built · Kit should: ship the page — its own TODO already states the hard part correctly, that a forced change must still require the old password or a borrowed unlocked session becomes a permanent takeover.

## 2026-09-13 · db/migrations/0014 (role `instructor`) · `people.view` at `assigned` makes a first session impossible
Expected: an instructor can open a session for any pilot on the fleet they are qualified on · Found: the seeded `instructor` role holds `people.view` at scope `assigned`, and `ASSIGNED_SQL` resolves that to "people on sessions where I am the assessor" — so the set an instructor may see is exactly the set they have already graded. A pilot they have never flown with cannot be put in a new session, and a newly qualified instructor sees nobody at all. The scope is self-referential: it can only ever confirm history, never start it · Done here: the role's `people.view` widened to `all` in `/admin/roles`, with the **fleet binding** on each instructor's grant (migration 0142) doing the narrowing — which is what fleet-bound grants were added for: one instructor role, the binding carries the fleet · Kit should: seed `instructor.people.view` at `all` and rely on the binding, or define a second scope that means "the roster I may fly with". `assigned` remains right for `sessions.grade`, `sign` and `finalize`, where the point is that you only act on a session you are on.

## 2026-09-15 · package.json `reset:clean` + db/migrations/0147 · a clean database cannot be migrated
Expected: `reset:clean` builds an instance from an empty database — that is what it is for, and the deploy path has no other way in, because the database is never copied · Found: the chain runs `migrate` **before** `config:load`, but `0147_assessor_materialised.sql` asserts `analytics_config` already holds `assessor_fairness.adjusted_delta.k_shrink`, which only `config:load` writes. On an empty database 0147 fails and rolls back, and every step after it never runs — so the failure presents as "no active competency framework", "unknown fleet A320", "No active user", "function refresh_assessor_analytics() does not exist", none of which name the cause. It was invisible on this machine for a week because the instance was built up over days: 0147 was applied long after config had been loaded, and `migrate` never had to reapply it. The first genuinely fresh build hit it — which on the demo-morning timetable would have been the morning of the demo · Done here: `reset:clean` now runs `migrate -- --to 0146`, then `config:load`, then `migrate` for the rest. `migrate.mjs` already supports `--to`; nothing else changed · Kit should: either make the config load a migration of its own so the ordering cannot be got wrong, or have 0147 seed the defaults it needs and let `config:load` overwrite them. A migration that depends on a script that runs after it is a build order nobody can follow.

## 2026-09-15 · lib/db.ts · the pool is opened by importing the module, so the app cannot be BUILT without a database
Expected: `next build` compiles the application; a connection string is a runtime concern · Found: `export const pool = global.__tmsPool ?? createPool()` runs at import, and `next build` imports every route module to collect page data — so the build threw "DATABASE_URL is not set" and the application could only be built somewhere it was also configured to run. That makes an image impossible: a build machine has no business holding a database credential, and a CI runner never will · Done here: a lazy `getPool()`, created on first use and parked on globalThis in development exactly as before; `export const pool` removed · Kit should: take it. A process that never queries — a build, a typecheck, a test importing a pure helper from a module that happens to sit beside this one — should not open a pool and should not need a connection string to exist.

## 2026-09-15 · package.json · every script is unrunnable anywhere without a .env file
Expected: migrations and seeds run in any environment the operator can configure · Found: all twenty scripts ran `node --env-file=.env`, and Node treats a MISSING --env-file as a fatal error. In a container there is no `.env` and there must not be — the environment comes from compose, and a secrets file has no business inside an image — so the entire instance build failed at its first step with an ENOENT naming the file and nothing about why it was absent · Done here: `--env-file-if-exists=.env` throughout. The workstation loads `.env` exactly as before; a container falls through to the process environment · Kit should: take it. This is the difference between a kit that can be deployed and one that can only be run where it was developed.

## 2026-09-15 · deploy/compose.yml · the documented `compose.yml + overlay` invocation has never worked
Expected: `docker compose -f compose.yml -f compose.prod.yml up -d`, as compose.yml and both overlays document · Found: the base file's `image: ${APP_IMAGE:?APP_IMAGE must come from compose.stage.yml or compose.prod.yml}` is unsatisfiable. Compose INTERPOLATES each file before MERGING the overlay onto it, so the required-variable error fires at parse time whatever the overlay says — and no overlay sets `APP_IMAGE` in any case: stage reads `APP_IMAGE_STAGE`, prod reads `APP_IMAGE_PROD`, which is the whole point of them · Done here: the base `app` service declares no `image:` at all. The overlay is the only place it is set, and a service with neither image nor build is invalid, so `compose.yml` alone is still refused — which is what the guard was for · Kit should: take it, and note that a guard written in a language's own syntax has to be tested in that language's order of operations.

## 2026-09-15 · deploy/ (no Dockerfile) + scripts/seed-framework.mjs · the framework document is a runtime dependency of the image
Expected: `kit-docs/` is documentation · Found: `seed-framework.mjs` re-parses `kit-docs/03_COMPETENCY_FRAMEWORK.md` and byte-matches all 73 observable behaviours against `db/seed/competency_framework.json` before it will write a row. The document is the AUTHORITY and the JSON is the copy — correct, and worth keeping — but it means an image that excludes documentation cannot seed a framework, and the failure is an ENOENT on a path that reads like a docs file nobody could need · Done here: `.dockerignore` excludes `kit-docs` and re-includes that one file; the Dockerfile copies it to `/app/kit-docs/` · Kit should: say so where the file lives, or move the authority into `db/seed/` and generate the document from it rather than the other way round.

## 2026-09-15 · 16 API routes · every POST redirect sends the browser to the server's own bind address
Expected: a form POST answers 303 and the browser lands on the page · Found: every redirect built its destination as `new URL(path, request.nextUrl.origin)`, and that origin is what the SERVER believes it is. In a container bound to 0.0.0.0:3000 the first crew session opened from the screen redirected to `http://0.0.0.0:3000/sessions/…`, which is not an address a browser can go to. Behind a reverse proxy it is the same story with a different address. The write had SUCCEEDED every time, so nothing in the logs looks wrong — the operator simply lands on a dead page after every save, sign-in, template publish and ticket · Done here: `lib/http.ts` — `seeOther(path)` emits a RELATIVE Location, which RFC 7231 allows and every browser resolves against the URL it actually requested; `pathWithQuery` composes the query without inventing an origin. All sixteen sites converted; no route asks for an origin any more · Kit should: take both helpers and forbid the pattern. A server that has to know its own public name has one more thing to get wrong in every new environment, and this one fails silently.

## 2026-09-15 · scripts/create-user.mjs · an instructor account cannot be the instructor of record
Expected: `npm run user -- a.betancur instructor --fleet A320` makes a working instructor login · Found: the script writes `person_id NULL` unconditionally and offers no way to set it, so the account is not linked to a roster row — and `/sessions/new` refuses it with "This account is not linked to a roster row". An account and a person are different things on purpose (an administrator has no roster row; a pilot has a roster row long before anyone gives them a login), but an INSTRUCTOR is named on sessions as a person, so an instructor account with no link is the one combination that cannot do its job · Done here: `--person <staff number | full name>`, resolved and validated before anything is written, refusing an ambiguous name rather than guessing — the wrong instructor on a signed record is not a mistake anybody notices until an audit. Re-running without `--person` never clears an existing link · Kit should: take it, and consider refusing to create an account holding an assessor role with no person at all.

## 2026-09-15 · app/(training)/sessions/[id]/grade/page.tsx · switching seats showed the other pilot's grades
Expected: the pilot tabs on a crewed session show that pilot's record · Found: the tabs were `<Link>`s differing only by the `person` search parameter, and the App Router's client cache keys its entries by ROUTE, not by search parameters. The second tab therefore reused the payload already rendered for the first: the header swapped to the other pilot's name while the grades, the per-pilot counter and the completeness list stayed those of the pilot just left — in both directions. Nothing was ever written to the wrong record; the DATA was right and only the screen was stale, which is worse rather than better, because there is nothing on the page to say so and the next grade is entered against what the instructor can see · Done here: a plain `<a>`, one full navigation, on the only page in the platform where confusing two people would be invisible · Kit should: take it. A crewed session is two records that must never be confused.

## 2026-09-15 · components/program/InstructorDemo.tsx · a finalised record was badged DRAFT
Expected: the badge says what the record is · Found: the status line was computed from `signed`, local component state that starts empty on every load, so a real record printed DRAFT across the top of a page whose signature chips — read from the server, a few centimetres below — said signed by three people and frozen. The local state only ever mattered for the in-memory walk-through, which has no server to ask · Done here: the badge reads `live.signing` when there is a live session, and gains FINALISED, which `live.signing.recordId` already knew · Kit should: take it.

## 2026-09-15 · app/(training)/sessions/[id]/page.tsx · a session-level outcome that nothing writes
Expected: the session card shows the outcome · Found: it rendered `sessions.outcome`, while `saveSessionFields` has always written the outcome to `session_subjects` — per pilot, because on a crewed session one pilot can pass and the other not. So the card read "not entered" on every session ever flown, including one whose two records both said PASS a moment after signing · Done here: the row is gone and the outcome is a column in the pilots table, where it is true and where a crew can disagree · Kit should: drop `sessions.outcome` or write it; a column that is only ever read is a lie with a schema.

## 2026-09-15 · app/(training)/sessions/new · one pilot per session
Expected: an instructor can open the session they are about to fly · Found: the form took one pilot and one seat, and said so at the foot of the page. The data model, the grading surface, the per-subject content hash and the seeded history all carry crews; only the way to START one was missing, so every crewed session in the system had been written directly into the database by the seeder · Done here: an optional second pilot, whose seat is the other seat by definition; every rule the first pilot passes is re-applied to the second from the database, and both seats are named in the audit entry · Kit should: take it. A simulator detail is normally flown by two, and a kit that can grade a crew but not open one is a kit whose first real session fails.

## 2026-09-15 · deploy/compose.yml · containers have no timezone, so the app renders UTC beside the browser's local time
Expected: a signature carries the time it was given · Found: a container has no timezone unless it is given one, so server-rendered timestamps came out in UTC while the browser rendered its own local time a few centimetres away — a grading banner reading 23:15 above a signature reading 18:28, for things that happened in that order five minutes apart · Done here: `TZ: ${TZ:-UTC}` on the app and the database, set per environment · Kit should: ship it. The database stores timestamptz in UTC whatever this says; this only decides what a human reads, and in front of a room that has to be the time on the wall.
